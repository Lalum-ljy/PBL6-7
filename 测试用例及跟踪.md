# 校园活动发布平台 测试用例及跟踪文档
---
## 文档基础信息
| 项目属性 | 详情说明 |
| :--- | :--- |
| 文档名称 | 校园活动发布平台 测试用例及跟踪文档 |
| 项目名称 | 校园活动发布平台 |
| 文档版本 | V1.0 |
| 生成依据 | 项目历次提交README.md全量迭代内容、功能需求说明 |
| 用例级别规则 | 0级=核心主流程（P0）、1级=核心异常/边界场景（P1）、2级=次要功能主流程（P2）、3级=次要异常/体验场景（P3）、4-5级=边缘/性能/兼容性场景 |

---
## 文档说明
本文档基于校园活动发布平台全量迭代提交记录生成，完整覆盖用户管理、活动管理、系统公告、缓存队列、智能助手等核心模块，包含功能主流程、异常场景、边界校验等全维度测试用例，可直接用于项目功能测试、回归测试执行与测试进度跟踪。

---
## 一、用户管理功能
| 功能名称 | 测试用例名称 | 级别(0-5) | 测试步骤 | 预期结果 | 测试结果 |
| :--- | :--- | :---: | :--- | :--- | :--- |
| 1.1 用户注册：收集用户基本信息（账号、姓名、用户类型、专业年级等）；验证用户名唯一性；密码BCrypt哈希加密存储；保存用户信息到sys_user表 | 正常用户注册 | 0 | 1、进入用户注册页面/调用注册接口<br/>2、输入合规的用户信息（唯一账号、密码、姓名、用户类型等必填项）<br/>3、提交注册请求，查询sys_user表校验数据 | 1、注册接口正常响应，返回注册成功提示<br/>2、用户信息正确写入sys_user表，密码为加密后的哈希值，非明文存储<br/>3、账号唯一性约束生效，无重复数据 | pass |
|  | 注册重复的用户名（账号） | 1 | 1、使用已存在于sys_user表的用户名进行注册<br/>2、提交请求后校验接口响应与数据库数据 | 1、接口返回“账号已存在”的明确提示，注册失败<br/>2、数据库无新增重复数据，原有用户数据不受影响 | 待测试 |
|  | 注册必填项为空校验 | 1 | 1、分别置空账号、密码、姓名等必填项，提交注册请求<br/>2、校验接口响应与数据写入情况 | 1、接口精准返回对应字段的必填校验提示，注册失败<br/>2、数据库无新增无效数据 | 待测试 |
|  | 密码格式合规性校验 | 2 | 1、输入不符合规则的密码（如长度不足、纯数字等），提交注册请求<br/>2、校验接口响应 | 接口返回密码格式要求提示，拦截非法注册请求，注册失败 | 待测试 |
| 1.2 用户登录：根据用户名+密码验证用户身份；登录成功生成token；登录后进入对应权限功能菜单 | 正常账号密码登录 | 0 | 1、进入登录页面/调用登录接口<br/>2、输入已注册的正确用户名和密码，提交登录请求<br/>3、校验登录响应与权限菜单 | 1、接口返回登录成功，生成有效token<br/>2、成功进入对应权限的功能菜单，用户身份信息匹配 | 待测试 |
|  | 错误密码登录校验 | 1 | 1、输入已注册的正确用户名+错误密码，提交登录请求<br/>2、校验接口响应与登录状态 | 1、接口返回“账号或密码错误”提示，登录失败<br/>2、不生成token，无法进入功能菜单 | 待测试 |
|  | 未注册账号登录校验 | 1 | 1、输入未注册的用户名+任意密码，提交登录请求<br/>2、校验接口响应 | 接口返回“账号不存在”提示，登录失败，无token生成 | 待测试 |
|  | 禁用账号登录校验 | 2 | 1、将sys_user表中对应账号的status置为0（禁用）<br/>2、使用该账号密码提交登录请求<br/>3、校验接口响应 | 接口返回“账号已被禁用”提示，登录失败，无token生成 | 待测试 |
| 1.3 用户数据管理：用户信息持久化存储；字段完整性与索引有效性校验 | 用户数据字段完整性校验 | 2 | 1、完成用户注册后，查询sys_user表对应数据<br/>2、校验所有字段的写入值、默认值、注释是否与设计一致 | 1、所有字段写入正确，默认值生效，无空值异常<br/>2、索引、唯一键约束正常生效，无数据异常 | 待测试 |


