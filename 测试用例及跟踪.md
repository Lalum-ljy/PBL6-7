# 校园活动发布平台 测试用例及跟踪文档
---
## 文档基础信息
| 项目属性 | 详情说明 |
| :--- | :--- |
| 文档名称 | 校园活动发布平台 测试用例及跟踪文档 |
| 项目名称 | 校园活动发布平台 |
| 文档版本 | V1.0 |
| 生成依据 | 项目历次提交README.md全量迭代内容、功能需求说明 |
| 用例级别规则 | 0级=核心主流程（P0）、1级=核心异常/边界场景（P1）、2级=次要功能主流程（P2）、3级=次要异常/体验场景（P3）、4-5级=边缘/性能/兼容性场景 |

---
## 文档说明
本文档基于校园活动发布平台全量迭代提交记录生成，完整覆盖用户管理、活动管理、系统公告、缓存队列、智能助手等核心模块，包含功能主流程、异常场景、边界校验等全维度测试用例，可直接用于项目功能测试、回归测试执行与测试进度跟踪。

---
## 一、用户管理功能
| 功能名称 | 测试用例名称 | 级别(0-5) | 测试步骤 | 预期结果 | 测试结果 |
| :--- | :--- | :---: | :--- | :--- | :--- |
| 1.1 用户注册：收集用户基本信息（账号、姓名、用户类型、专业年级等）；验证用户名唯一性；密码BCrypt哈希加密存储；保存用户信息到sys_user表 | 正常用户注册 | 0 | 1、进入用户注册页面/调用注册接口<br/>2、输入合规的用户信息（唯一账号、密码、姓名、用户类型等必填项）<br/>3、提交注册请求，查询sys_user表校验数据 | 1、注册接口正常响应，返回注册成功提示<br/>2、用户信息正确写入sys_user表，密码为加密后的哈希值，非明文存储<br/>3、账号唯一性约束生效，无重复数据 | pass |
|  | 注册重复的用户名（账号） | 1 | 1、使用已存在于sys_user表的用户名进行注册<br/>2、提交请求后校验接口响应与数据库数据 | 1、接口返回“账号已存在”的明确提示，注册失败<br/>2、数据库无新增重复数据，原有用户数据不受影响 | 待测试 |
|  | 注册必填项为空校验 | 1 | 1、分别置空账号、密码、姓名等必填项，提交注册请求<br/>2、校验接口响应与数据写入情况 | 1、接口精准返回对应字段的必填校验提示，注册失败<br/>2、数据库无新增无效数据 | 待测试 |
|  | 密码格式合规性校验 | 2 | 1、输入不符合规则的密码（如长度不足、纯数字等），提交注册请求<br/>2、校验接口响应 | 接口返回密码格式要求提示，拦截非法注册请求，注册失败 | 待测试 |
| 1.2 用户登录：根据用户名+密码验证用户身份；登录成功生成token；登录后进入对应权限功能菜单 | 正常账号密码登录 | 0 | 1、进入登录页面/调用登录接口<br/>2、输入已注册的正确用户名和密码，提交登录请求<br/>3、校验登录响应与权限菜单 | 1、接口返回登录成功，生成有效token<br/>2、成功进入对应权限的功能菜单，用户身份信息匹配 | 待测试 |
|  | 错误密码登录校验 | 1 | 1、输入已注册的正确用户名+错误密码，提交登录请求<br/>2、校验接口响应与登录状态 | 1、接口返回“账号或密码错误”提示，登录失败<br/>2、不生成token，无法进入功能菜单 | 待测试 |
|  | 未注册账号登录校验 | 1 | 1、输入未注册的用户名+任意密码，提交登录请求<br/>2、校验接口响应 | 接口返回“账号不存在”提示，登录失败，无token生成 | 待测试 |
|  | 禁用账号登录校验 | 2 | 1、将sys_user表中对应账号的status置为0（禁用）<br/>2、使用该账号密码提交登录请求<br/>3、校验接口响应 | 接口返回“账号已被禁用”提示，登录失败，无token生成 | 待测试 |
| 1.3 用户数据管理：用户信息持久化存储；字段完整性与索引有效性校验 | 用户数据字段完整性校验 | 2 | 1、完成用户注册后，查询sys_user表对应数据<br/>2、校验所有字段的写入值、默认值、注释是否与设计一致 | 1、所有字段写入正确，默认值生效，无空值异常<br/>2、索引、唯一键约束正常生效，无数据异常 | 待测试 |
\---

\## 二、活动管理功能

| 功能名称 | 测试用例名称 | 级别(0-5) | 测试步骤 | 预期结果 | 测试结果 |

| :--- | :--- | :---: | :--- | :--- | :--- |

| 2.1 活动新增：创建活动填写基础信息；支持封面图片上传；活动信息写入sys\_activity表 | 正常新增活动（无封面图） | 0 | 1、登录有权限的账号，进入活动创建页面/调用新增活动接口<br/>2、填写合规的活动必填信息（名称、描述、起止时间、创建人等）<br/>3、提交请求，查询sys\_activity表校验数据 | 1、接口返回创建成功，活动信息正确写入sys\_activity表<br/>2、活动默认状态、热门状态等默认值正确生效，字段无缺失 | 待测试 |

|  | 新增活动+封面图片上传 | 0 | 1、调用POST/api/updown/img接口上传合规的封面图片<br/>2、获取图片URL后，调用POST/api/activity接口提交完整活动信息<br/>3、校验图片存储与活动数据写入情况 | 1、图片成功上传至指定路径src/mian/resources/static/img，返回有效URL<br/>2、活动信息完整写入sys\_activity表，cover\_url字段匹配上传的图片地址<br/>3、活动创建成功，无数据异常 | 待测试 |

|  | 新增活动必填项为空校验 | 1 | 1、分别置空活动名称、起止时间、创建人等必填项，提交新增请求<br/>2、校验接口响应与数据写入情况 | 1、接口返回对应字段的必填校验提示，活动创建失败<br/>2、数据库无新增无效活动数据 | 待测试 |

|  | 活动时间逻辑校验 | 1 | 1、填写活动结束时间早于开始时间，提交新增请求<br/>2、校验接口响应 | 接口返回“结束时间不能早于开始时间”提示，拦截非法创建请求，活动创建失败 | 待测试 |

|  | 非法格式图片上传校验 | 2 | 1、调用图片上传接口，上传非图片格式文件、超大体积图片<br/>2、校验接口响应与文件存储情况 | 1、接口返回文件格式/大小超限提示，上传失败<br/>2、非法文件不会存入服务器指定路径，无异常垃圾文件 | 待测试 |

| 2.2 活动修改：根据活动ID修改活动信息；支持更新热门状态、封面图等 | 正常修改活动基础信息 | 0 | 1、获取已存在的活动ID，调用修改接口更新活动名称、描述、起止时间等信息<br/>2、提交请求，查询数据库校验更新结果 | 1、接口返回修改成功，sys\_activity表对应活动的字段同步更新<br/>2、update\_time字段自动刷新，其他未修改字段保持不变 | 待测试 |

|  | 修改活动热门状态 | 0 | 1、获取已存在的活动ID，调用修改接口更新hot\_status为1（热门）<br/>2、提交请求，校验数据库更新结果 | 接口返回修改成功，对应活动的hot\_status字段成功更新，无其他数据异常 | 待测试 |

|  | 修改不存在的活动ID | 1 | 1、使用数据库中不存在的活动ID，提交修改请求<br/>2、校验接口响应 | 接口返回“活动不存在”提示，修改失败，无数据库数据变更 | 待测试 |

| 2.3 活动删除：根据活动ID删除活动 | 正常删除活动 | 0 | 1、获取已存在的活动ID，调用删除接口提交请求<br/>2、查询sys\_activity表校验数据 | 1、接口返回删除成功，对应活动数据从表中移除<br/>2、无其他活动数据受影响 | 待测试 |

|  | 删除不存在的活动ID | 1 | 1、使用数据库中不存在的活动ID，提交删除请求<br/>2、校验接口响应 | 接口返回“活动不存在”提示，删除失败，无数据库数据变更 | 待测试 |

| 2.4 活动查询：根据活动ID查询详情；查询所有活动列表 | 根据活动ID查询活动详情 | 0 | 1、获取已存在的活动ID，调用详情查询接口<br/>2、校验返回数据与数据库数据一致性 | 1、接口返回成功，活动详情字段完整、数据准确，与数据库一致<br/>2、不存在的ID查询返回明确的“活动不存在”提示 | 待测试 |

|  | 查询所有活动列表 | 0 | 1、调用活动列表查询接口，不传入任何筛选条件<br/>2、校验返回数据与数据库全量活动数据一致性 | 接口返回成功，列表包含数据库中所有有效活动数据，无遗漏、无多余数据 | 待测试 |

| 2.5 活动筛选：按时间范围、活动状态、热门状态筛选 | 按活动状态筛选 | 0 | 1、分别传入状态值0（未开始）、1（进行中）、2（已结束）、3（已取消），调用筛选接口<br/>2、校验返回列表的状态字段 | 接口返回成功，列表仅包含对应状态的活动数据，无状态不匹配的数据 | 待测试 |

|  | 按时间范围筛选活动 | 0 | 1、传入合法的起止时间范围，调用时间筛选接口<br/>2、校验返回列表的活动起止时间是否符合筛选范围 | 接口返回成功，列表仅包含在筛选时间范围内的活动数据，无超出范围的数据 | 待测试 |

|  | 按热门状态筛选活动 | 0 | 1、传入hot\_status=1（热门），调用筛选接口<br/>2、校验返回列表的热门状态字段 | 接口返回成功，列表仅包含热门状态为1的活动数据，无非热门数据混入 | 待测试 |

| 2.6 活动搜索：按标题、标题+内容、发起人模糊搜索 | 按活动标题模糊搜索 | 0 | 1、传入活动标题的关键词，调用搜索接口<br/>2、校验返回列表的活动名称是否包含对应关键词 | 接口返回成功，列表仅包含标题含搜索关键词的活动数据，模糊匹配生效 | 待测试 |

|  | 按标题+内容模糊搜索 | 0 | 1、传入活动标题/描述中的关键词，调用搜索接口<br/>2、校验返回列表的标题/描述是否包含对应关键词 | 接口返回成功，列表包含标题或描述含搜索关键词的活动数据，匹配规则生效 | 待测试 |

|  | 按发起人模糊搜索（两表联合） | 0 | 1、传入发起人姓名/账号关键词，调用搜索接口<br/>2、校验返回列表的创建人是否匹配，两表关联是否正常 | 接口返回成功，列表仅包含创建人匹配关键词的活动数据，两表联合查询无异常 | 待测试 |


